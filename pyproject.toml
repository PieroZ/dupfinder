[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dupfinder"
version = "0.1.0"
description = "Duplicate file finder with cache"
requires-python = ">=3.10"

[project.scripts]
dupfinder = "dupfinder.cli:main"
